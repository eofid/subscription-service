# Subscription Service (Go Middle)

RESTful API сервис для агрегации данных о подписках пользователей.
Реализован с использованием принципов **Clean Architecture**.

## 📋 Требования (Техническое задание)
- CRUDL операции над подписками.
- Агрегация стоимости подписок пользователя.
- Работа с базой данных PostgreSQL (драйвер pgx).
- Graceful Shutdown.
- Конфигурация через `.env`.
- Docker Compose для запуска.

## 🛠 Технологический стек
- **Язык:** Go 1.25
- **Архитектура:** Clean Architecture (Transport -> Service -> Repository)
- **База данных:** PostgreSQL 15
- **Роутер:** Chi
- **Логирование:** log/slog (JSON structured logging)
- **Контейнеризация:** Docker & Docker Compose

## 🚀 Запуск проекта

Для запуска требуется установленный Docker.

```bash
docker compose up --build

Сервис запустится на порту 8090.
🔌 API Endpoints
Метод	URL	Описание
POST	/subscriptions	Создать подписку
GET	/subscriptions/total?user_id={id}	Получить сумму расходов
PUT	/subscriptions/{id}	Обновить данные подписки
DELETE	/subscriptions/{id}	Удалить подписку